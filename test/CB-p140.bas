10 A$ = "A"
20 B$ = "B"
30 IF A$ = "A" AND B$ = "B" THEN ? "TRUE" ELSE ? "FASLE"
40 IF A$ = "A" OR B$ = "C" THEN ? "TRUE" ELSE ? "FASLE"
50 IF A$ = "C" OR B$ = "B" THEN ? "TRUE" ELSE ? "FASLE"
60 A = RND(100) - 50
70 ? "SIGN OF"A"IS"SGN(A)
80 ? "ABS OF"A"IS"ABS(A)
90 ? STR$(A) + " IS NOW A STRING"

